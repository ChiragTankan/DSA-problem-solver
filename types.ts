
// This file can be used for shared TypeScript types and interfaces.
// For this application, most types are co-located with their components or services.

export interface ExampleType {
  // Example structure if needed in the future
  id: string;
  value: string;
}
